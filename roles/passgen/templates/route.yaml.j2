apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: passgen
  name: passgen
spec:
  host: "{{ hostname }}"
  port:
    targetPort: 8443-tcp
  tls:
    termination: passthrough
  to:
    kind: Service
    name: passgen
    weight: 100
  wildcardPolicy: None

